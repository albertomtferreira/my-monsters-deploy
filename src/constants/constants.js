const MESSAGES = { 
  'MESSAGE_SEARCHBOX': 'Search Monsters',
  'ERROR_MESSAGE': 'I have an error',
  'HOME': [{
      'HEADING': 'welcome to the home'
   }]
}

// Object.freeze(MESSAGES);

export default MESSAGES;